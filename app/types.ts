export type Tracker = {
  id: any;
  name: string;
  startTime: string;
  description: string;
  elapsedTime: number;
  paused: boolean;
  finished: boolean;
  entryDate: string;
  userId: string;
};
